### 更新日志
#### ToDo
  - [x] 从文件解析人设
  - [x] 提供tool，让ai可以直接创建/修改人格
  - [x] 支持向用户发送文件形式的人设文件

#### v1.4.7
  - 修复裸 `pp` 状态指令与 `pp` 命令组同名注册导致子命令被抢占的问题

#### v1.4.6
  - 修复关闭切换提示后 `pp <人格ID>` 快捷切换可能继续触发默认 LLM 的问题
  - 统一 `admin_commands` 默认权限配置与 help / README / WebUI 配置说明
  - 统一未找到人格时的中文错误提示，避免直接暴露底层英文异常
  - 支持直接使用实时全局序号执行切换、查看、导出、更新、头像上传和删除，不再依赖先执行 `list`
  - `list [文件夹]` 改为显示全局序号，确保任意列表中的编号含义一致
  - 移除最近列表编号缓存，避免人格增删后出现过期编号
  - 抽取人格列表排序逻辑，确保 `list` 展示顺序与序号解析顺序一致
  - 新增 `pp switch <人格>` 显式切换指令，可切换与子命令同名的人格
  - 优化指令注册实现，统一权限检查入口并稳定 `list` 的文件夹路径参数类型
  - 优化裸 `pp` 指令，不再显示参数不足，而是展示当前人格并提示使用 `pp help`
  - 修复裸 `pp` 同时命中命令组导致仍返回“参数不足”的问题

#### v1.4.5
  - 将全部 LLM Tool 名称由 `persona_plus_*` 简化为 `persona_*`
  - 更新函数工具说明文档，统一使用 `persona_create`、`persona_update` 等新名称
  - 卸载函数工具时兼容清理旧版 `persona_plus_*` 工具注册

#### v1.4.4
  - 修复 `pp export` 导出文件只出现发送日志但平台未实际收到的问题
  - 导出文件发送改为复用当前会话主动发送链路，提升 OneBot 与 WebChat 等平台兼容性
  - 统一指令导出与 `persona_plus_export` 函数工具的文件发送逻辑

#### v1.4.3
  - 重构 Persona+ 核心架构，拆分人设服务、渲染与引用解析模块
  - 优化指令处理流程与 LLM Tool 集成，提升可维护性与扩展性
  - 统一日志与注释风格，改进代码结构

#### v1.4.2
  - 新增 `pp export <人格>` 指令，可将人格 System Prompt 导出为 Markdown 文件发送
  - 新增 `persona_plus_export` 函数工具，支持通过自然语言导出人设文件

#### v1.4.1
  - 重构人格创建与更新逻辑，统一指令和 LLM Tool 的底层接口
  - LLM Tool 支持完整创建/更新预设对话、工具/MCP 工具、Skills 和自定义错误回复
  - 重写 README，补充表格化能力说明、配置项和函数工具用法

#### v1.4.0
  - 优化指令与 LLM Tool 的返回文本排版，统一 help / list / view 的展示风格
  - list 改为树形结构显示，支持文件夹层级与根目录统一展示
  - list 增加序号，支持通过序号执行切换、查看、更新、头像上传、删除等操作
  - 修复 LLM Tool 与指令在详情展示、序号解析上的一致性问题

#### v1.3.6
  - 修改LLM Tool注册方式，现在在配置文件中关闭的函数工具将不再显示在AstrBot框架中

#### v1.3.5
  - 修复读取文件设定人设时丢失换行的问题

#### v1.3.4
  - 修复 SenderScopedSessionFilter() takes no arguments 问题
  - 修复 llm tool 调用失败问题

#### v1.3.3
  - **此版本相比`1.3.2`无功能更新，主要是优化代码实现**
  - 优化日志系统
  - 优化代码性能
  - 优化异常处理
  - 优化缓存控制
  - 统一注释风格
  - 移除多余的llm_tools.py文件
  
#### v1.3.2
  - 添加指令的用户识别，防止使用指令时被其他用户消息打断
  - 适配高版本的人格设定文件夹
  - 添加llm tool,便于使用自然语言调用插件功能
  - 更新webui面板配置项
  - 更新help说明文本，自动读取当先指令前缀
  
#### v1.3.1
  - 修复切换人设后，未清理聊天增强历史的问题(开启自动reset的情况下)
  - 拆分代码
#### v1.3
  - 细化权限管理，可针对每一个指令设置权限，统一权限验证
  - 优化代码，减少重复逻辑
  - 修复 windows下无法修改头像的问题，使用base64和file url传输图片
  - 修复 手机对话时，无法正常创建、更新人格的问题（过滤"对方正在输入中"的输入状态）

#### v1.2
  - 从文本文件解析人设
  
#### v1.1
  - 添加插件logo
  - 添加更改群昵称的功能
  
#### v1.0
  - 实现插件基本功能